export const JWT_RESPONSE_CODE = {
    ERROR: {
        INVALID: "invalid_jwt_token",
        INVALID_FORMAT: "invalid_jwt_token_format",
        EXPIRED: "jwt_token_expired",
        INVALID_CLAIMS: "invalid_jwt_token_claims",
        REQUIRED: "jwt_token_required"
    }
}